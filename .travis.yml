language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t shubhankergoyal/docker-react -f Dockerfile.dev .

script:
   docker run -e CI=true shubhankergoyal/docker-react npm run test
